@model IEnumerable<MyFood.Models.BuffetType>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>أنواع الضيافة</h2>

<br />
<p>
    @Html.ActionLink("إضافة نوع جديد", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.buffet_type_name)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.buffet_type_name)
        </td>
        <td>
            @Html.ActionLink("تعديل", "Edit", new { id=item.buffet_type_id }) |
            @Html.ActionLink("تفاصيل", "Details", new { id=item.buffet_type_id }) 
            @*@Html.ActionLink("Delete", "Delete", new { id=item.buffet_type_id })*@
        </td>
    </tr>
}

</table>
