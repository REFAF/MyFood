@model IEnumerable<MyFood.Models.Tool>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>أصناف أدوات التعبئة</h2>

<p>
    @Html.ActionLink("إضافة صنف جديد", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.tool_name)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.tool_name)
        </td>
        <td>
            @Html.ActionLink("تعديل", "Edit", new { id=item.tool_id }) @*|
            @Html.ActionLink("Details", "Details", new { id=item.tool_id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.tool_id })*@
        </td>
    </tr>
}

</table>
