@model IEnumerable<MyFood.Models.Neighborhood>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>الأحياء</h2>

<p>
    @Html.ActionLink("إضافة حي جديد", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Neighborhood_name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.direction_id)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
<tr>
    <td>
        @Html.DisplayFor(modelItem => item.Neighborhood_name)
    </td>
    <td>
        @Html.DisplayFor(modelItem => item.Direction.direction_name)
    </td>
    <td>
        @Html.ActionLink("تعديل", "Edit", new { id = item.Neighborhood_id }) @*|
            @Html.ActionLink("Details", "Details", new { id=item.Neighborhood_id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Neighborhood_id })*@
    </td>
</tr>
}

</table>
